name: Deploy to ec2

on:
  push:
    branches: [ main ]

jobs:
  reset:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
   
      

